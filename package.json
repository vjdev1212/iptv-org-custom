{
  "name": "iptv-org-filter",
  "version": "1.0.0",
  "description": "A custom playlist creator from IPTV org",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "deploy:staging": "wrangler deploy --env staging",
    "tail": "wrangler tail",
    "test": "wrangler dev --local"
  },
  "devDependencies": {
    "wrangler": "^4.58.0"
  }
}
